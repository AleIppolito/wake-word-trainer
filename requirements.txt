# ─────────────────────────────────────────────────────────────────────────────
# Custom Wake Word - openWakeWord trainer
# Python 3.12 + CUDA 13.0
#
# Setup (one-time):
#   bash setup.sh
#
# NOT included here (installed after git clone by 00_download.py):
#   - openwakeword   → pip install -e ./openwakeword --no-deps
# ─────────────────────────────────────────────────────────────────────────────

--extra-index-url https://download.pytorch.org/whl/cu130

# ── CUDA-pinned (must match CUDA 13.0) ───────────────────────────────────────
torch==2.11.0+cu130
torchaudio==2.11.0+cu130
torchvision==0.26.0+cu130

# ── audio / ML ────────────────────────────────────────────────────────────────
acoustics
audiomentations
librosa
numpy
scikit-learn
scipy
soundfile
speechbrain
torch-audiomentations
torchinfo
torchmetrics
tqdm

# ── data ──────────────────────────────────────────────────────────────────────
datasets
huggingface_hub
mutagen
pandas
requests

# ── TensorFlow (needed by onnx2tf for TFLite conversion) ─────────────────────
tensorflow
tf-keras

# ── ONNX / TFLite ─────────────────────────────────────────────────────────────
ai-edge-litert
onnx
onnx2tf
onnxoptimizer
onnxruntime-gpu
onnxscript
onnxsim-prebuilt
sne4onnx
sng4onnx

# ── openWakeWord deps ─────────────────────────────────────────────────────────
pronouncing==0.3.0
PyYAML
