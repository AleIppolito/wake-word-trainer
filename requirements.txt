# ─────────────────────────────────────────────────────────────────────────────
# Custom Wake Word - openWakeWord trainer
# Python 3.12 + CUDA 13.0
#
# Install:
#   python -m venv venv
#   source venv/bin/activate
#   pip install -r requirements.txt
#   python 00_download.py       # clone repos + download datasets (~17 GB, once)
#   python 01_fix_n_patch.py    # apply post-install patches
#   python 02_training.py
#
# NOT included here (must be installed after git clone):
#   - openwakeword   → pip install -e ./openwakeword --no-deps  (done by 00_download.py)
# ─────────────────────────────────────────────────────────────────────────────

--extra-index-url https://download.pytorch.org/whl/cu130

# ── CUDA-pinned (must match CUDA 13.0) ───────────────────────────────────────
torch==2.11.0+cu130
torchaudio==2.11.0+cu130
torchvision==0.26.0+cu130

# ── audio / ML ────────────────────────────────────────────────────────────────
acoustics
audiomentations
librosa
numpy
scikit-learn
scipy
soundfile
speechbrain
torch-audiomentations
tqdm

# ── data ──────────────────────────────────────────────────────────────────────
datasets
huggingface_hub
pandas

# ── ONNX / TFLite ─────────────────────────────────────────────────────────────
ai-edge-litert
onnx
onnx2tf
onnxoptimizer
onnxruntime-gpu
onnxscript
onnxsim-prebuilt
sne4onnx
sng4onnx

# ── TTS (Italian wake word generation) ───────────────────────────────────────
piper-phonemize-cross
piper-tts

# ── openWakeWord deps ─────────────────────────────────────────────────────────
pronouncing==0.3.0
PyYAML
